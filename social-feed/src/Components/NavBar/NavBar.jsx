import React, { useState } from "react";

const NavBar = (props) => {
    return (
        <nav className="nav-container">
            <h3>
                Social <small className="mutedTextColor">Feed</small>
            </h3>
        </nav>
    );
};

export default NavBar;
